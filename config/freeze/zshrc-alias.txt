# -----------------------------------------------------------------------------
# 卡死安全重启：TTY(Ctrl+Alt+F3) 里敲 freeze 回车即可
#   保存数据 → 干净重启；systemd 卡住自动走内核级硬重启
#   脚本：~/.local/bin/reboot-safe（/etc/sudoers.d/reboot-safe 已配免密码）
# -----------------------------------------------------------------------------
alias freeze='sudo ~/.local/bin/reboot-safe'
